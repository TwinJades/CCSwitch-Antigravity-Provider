# Database migrations

Alembic migrations for the local Gateway SQLite database. Phase 1 contains an intentionally schema-neutral baseline; business tables are introduced only by their authorized phases.
